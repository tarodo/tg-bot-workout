def concat_new(message: str) -> str:
    """
    Concatenates the input message with the string " new".

    Args:
        message: Input message from the user

    Returns:
        String with " new" appended at the end
    """
    return f"{message} new"
